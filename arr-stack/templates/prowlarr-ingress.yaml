{{- if and .Values.prowlarr.enabled .Values.prowlarr.ingress.enabled .Values.ingress.enabled }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "arr-stack.componentName" (dict "component" "prowlarr" "context" .) }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
    {{- include "arr-stack.componentLabels" "prowlarr" | nindent 4 }}
  annotations:
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
    caddy.reverse_proxy: "http://{{ include "arr-stack.componentName" (dict "component" "prowlarr" "context" .) }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.prowlarr.service.port }}"
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
  - hosts:
    - {{ .Values.prowlarr.ingress.hostname }}.{{ .Values.ingress.domain }}
    {{- if .Values.ingress.tls.secretName }}
    secretName: {{ .Values.ingress.tls.secretName }}-prowlarr
    {{- end }}
  {{- end }}
  rules:
  - host: {{ .Values.prowlarr.ingress.hostname }}.{{ .Values.ingress.domain }}
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: {{ include "arr-stack.componentName" (dict "component" "prowlarr" "context" .) }}
            port:
              number: {{ .Values.prowlarr.service.port }}
{{- end }}
