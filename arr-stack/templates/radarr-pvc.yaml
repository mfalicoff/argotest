{{- if and .Values.radarr.enabled .Values.radarr.persistence.config.enabled }}
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "arr-stack.componentName" (dict "component" "radarr-config" "context" .) }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
    {{- include "arr-stack.componentLabels" "radarr" | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  {{- if .Values.radarr.persistence.config.storageClass }}
  storageClassName: {{ .Values.radarr.persistence.config.storageClass }}
  {{- else if .Values.global.storageClass }}
  storageClassName: {{ .Values.global.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.radarr.persistence.config.size }}
{{- end }}
