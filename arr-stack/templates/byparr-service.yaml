{{- if .Values.byparr.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "arr-stack.componentName" (dict "component" "byparr" "context" .) }}
  labels:
    {{- include "arr-stack.labels" . | nindent 4 }}
    {{- include "arr-stack.componentLabels" "byparr" | nindent 4 }}
spec:
  type: {{ .Values.byparr.service.type }}
  ports:
  - port: {{ .Values.byparr.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "arr-stack.selectorLabels" . | nindent 4 }}
    {{- include "arr-stack.componentLabels" "byparr" | nindent 4 }}
{{- end }}
